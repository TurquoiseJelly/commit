site:
  title: "FlatPress"
  description: "A lightweight, Git-backed, Markdown-driven static site."
  baseUrl: "/"
  language: "en"
theme: "midnight"
blog:
  postsPerPage: 5
  feedLimit: 20
build:
  contentDir: "content"
  outputDir: "dist"
  staticDir: "static"
